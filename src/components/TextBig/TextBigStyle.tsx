import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        width: '100%',
    },
    title:{
        fontWeight: 'bold',
        marginBottom: 5
    },
    button: {
        borderColor: 'black',
        paddingTop: 10,
        paddingLeft: 10,
        borderWidth: 1,
        textAlignVertical: 'top',
        borderRadius: 8
    }
});